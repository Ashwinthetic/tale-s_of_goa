# Frontend

A Vite + React + TypeScript single-page app.

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:5173](http://localhost:5173) with your browser to see the result.

The backend FastAPI server is expected at `http://localhost:8000` (override with a `VITE_API_URL` env var).
