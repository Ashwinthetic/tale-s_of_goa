# Backend API Service

Express-based backend API service for **Tales of Goa**.

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the development server:
   ```bash
   npm run dev
   ```

3. Endpoints:
   - `GET /` - Root status
   - `GET /api/health` - Health check status
